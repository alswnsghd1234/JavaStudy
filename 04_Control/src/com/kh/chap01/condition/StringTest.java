package com.kh.chap01.condition;

public class StringTest {

	//기본 자료형 : boolean,char,byte, short, int, float, double, long
	// - > == != 기본자료형은 동등비교 연산자 사용 가능
	
	// 참조자료형 : String
	// 동등비교가 불가능하다.
	// 왜? 실제 변수에 담기는 값이 주소값(해당하는 값이 실제로 담긴 메모리 영역)이기 때문에.
	// 그래서 문자열을 비교할 땐 equals 메소드를 사용해서 문자열 자체를 비교해야한다.
	//[표현법] 문자열. equals("비교할 문자열")
}
