package com.kh.chap04.constructor.model.vo;

public class User {

	//생성자부
	
	/*
	 * 생성자 : 메소드 이름이 클래스 이름과 동일하고 리턴 자료형이 업슨ㄴ 메소드 (반환형이 없다)
	 * 생성자가 왜 필요한가? 객체를 생성할 때 메모리 공간을 할당 받아야되는 데 heap영역엔 빈공간이 있을 수 없어서
	 * 기본값이 라도 넣어주는 기본 생성자가 만들어지고 또는 임의로 초기값을 설정하고 싶을때 매개변수 생성자를 넣어 설정 가능하다.
	 * 
	 * 객체 생성 구문중 new부터
	 * ex) User u = new * User();
	 * 
	 * [표현법]
	 * 
	 *public 클래스명(매개변수(생략가능)){
	 *	만약 매개변수 생성자라면
	 *	필드 초기화 구문이 들어간다
	 *	ex)this.userId=userId; (매개변수가 userId일때)
	 *}
	 * 
	 * 
	 * 
	 * */
	
	
}
